#include "main.h"
/**
 * add - add 2 numbers
 * Description: Adds 2 numbers and returns the result
 * @a: &st number
 * @b: second number
 * Return: result
 */
int add(int a, int b)
{
	return (a + b);
}
